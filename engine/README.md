# DBSP Engine

`engine` is the core incremental computation module. It contains the circuit model, operators,
compiler, executor/runtime, and Z-set data structures used by higher-level modules.

## Quick Start

```bash
go test ./...
```

From repository root:

```bash
GOWORK=off go test ./...
```
