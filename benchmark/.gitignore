# Local tool binaries (make envtest) and the managed envoy-gateway
# clone (make eg).
bin/
.cache/

# Per-experiment measurement output (regenerated by each run.sh; the
# tracked results.org renders them into charts).
results/

# Rendered charts and LaTeX build leftovers.
*.pdf
*.png
*.tex
*.log
*.aux
*.out
ltximg/
auto/
